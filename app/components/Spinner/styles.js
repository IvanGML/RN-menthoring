import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    spinner: {
        height: '80%',
        justifyContent: 'center',
        alignItems: 'center'
    }
});

export { styles };


